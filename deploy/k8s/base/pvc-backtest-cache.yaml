apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backtest-cache
  labels:
    app.kubernetes.io/name: backtest-cache
    app.kubernetes.io/component: backtest
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
