apiVersion: v1
kind: ConfigMap
metadata:
  name: backtest-config
  labels:
    app.kubernetes.io/name: backtest-config
    app.kubernetes.io/component: backtest
data:
  BACKTEST_RESULTS_DIR: "/data/backtest-results"
  BACKTEST_CACHE_DIR: "/data/cache"
  ARGO_NAMESPACE: "backtest-workflows"
  ARGO_SERVER_URL: "http://argo-workflows-server.argo.svc.cluster.local:2746"
  ARGO_WORKFLOW_SERVICE_ACCOUNT: "backtest-workflow"
