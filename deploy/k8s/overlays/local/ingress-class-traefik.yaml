# k3s/Rancher Desktop Traefik watches Ingresses with ingressClassName: traefik only
# after this class exists (not created by default on all k3s versions).
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: traefik
spec:
  controller: traefik.io/ingress-controller
